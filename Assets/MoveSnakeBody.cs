using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveSnakeBody : MonoBehaviour
{
    // Start is called before the first frame update
    void TheStart()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
